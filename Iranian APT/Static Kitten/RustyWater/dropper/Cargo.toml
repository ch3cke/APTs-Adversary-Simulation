[package]
name = "dropper"
version = "0.1.0"
edition = "2021"

[dependencies]
winapi = { version = "0.3.9", features = [
    "winreg", "winnt", "minwindef", "handleapi", "processthreadsapi"
] }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
